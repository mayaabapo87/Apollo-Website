<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Partners</title>

        <!-- Bootstrap CSS -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
            integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">

        <!-- Bootstrap JavaScript -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
            crossorigin="anonymous">
        </script>

        <script src="https://cdnjs.cloudflare.com/ajax/libs/pdf.js/2.8.335/pdf.js"></script>
    </head>

    <body>
        <nav class="navbar navbar-expand-sm navbar-dark bg-dark">
            <div class="container">
                <div class="collapse navbar-collapse show" id="navbarDark">
                    <ul class="navbar-nav me-auto mb-2 mb-xl-0">
                        <li class="nav-item">
                            <a class="nav-link" href="/resume">
                                Resumes
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/career">
                                Careers
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link active" href="/partner">
                                Partners
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/story">
                                Stories
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link " href="/service">
                                Services
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link " href="/project">
                                Projects
                            </a>
                        </li>
                    </ul>
                    <form class="d-flex" id="searchForm">
                        <input class="form-control mx-1" type="search" placeholder="Search" aria-label="Search"
                            id="searchInput" name="search">
                        <button class="btn btn-outline-light mx-1" type="submit" id="searchButton">
                            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-search"viewBox="0 0 16 16">
                                <path d="M11.742 10.344a6.5 6.5 0 1 0-1.397 1.398h-.001c.03.04.062.078.098.115l3.85 3.85a1 1 0 0 0 
                                1.415-1.414l-3.85-3.85a1.007 1.007 0 0 0-.115-.1zM12 6.5a5.5 5.5 0 1 1-11 0 5.5 5.5 0 0 1 11 0z" />
                            </svg>
                        </button>
                        <button class="btn btn-danger mx-1" type="button" id="resetSearchButton">
                            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor"
                                class="bi bi-x-circle" viewBox="0 0 16 16">
                                <path d="M8 15A7 7 0 1 1 8 1a7 7 0 0 1 0 14zm0 1A8 8 0 1 0 8 0a8 8 0 0 0 0 16z" />
                                <path d="M4.646 4.646a.5.5 0 0 1 .708 0L8 7.293l2.646-2.647a.5.5 0 0 1 .708.708L8.707 8l2.647 
                                2.646a.5.5 0 0 1-.708.708L8 8.707l-2.646 2.647a.5.5 0 0 1-.708-.708L7.293 8 4.646 
                                5.354a.5.5 0 0 1 0-.708z" />
                            </svg>
                        </button>
                        <button class="btn btn-danger" id="logout-button">
                            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-box-arrow-in-left" viewBox="0 0 16 16">
                                <path fill-rule="evenodd" d="M10 3.5a.5.5 0 0 0-.5-.5h-8a.5.5 0 0 0-.5.5v9a.5.5 0 0 0 .5.5h8a.5.5 0 0 0 .5-.5v-2a.5.5 0 0 1 1 0v2A1.5 1.5 0 0 1 9.5 14h-8A1.5 1.5 0 0 1 0 12.5v-9A1.5 1.5 0 0 1 1.5 2h8A1.5 1.5 0 0 1 11 3.5v2a.5.5 0 0 1-1 0v-2z"/>
                                <path fill-rule="evenodd" d="M4.146 8.354a.5.5 0 0 1 0-.708l3-3a.5.5 0 1 1 .708.708L5.707 7.5H14.5a.5.5 0 0 1 0 1H5.707l2.147 2.146a.5.5 0 0 1-.708.708l-3-3z"/>
                            </svg>
                        </button>
                    </form>
                </div>
            </div>
        </nav>

        <div class="container mt-3">
            <table class="table table-hover caption-top">
                <div class="caption my-1" id="partnerCaption">
                    <button class="btn btn-outline-success add-button" data-bs-target="#partnerModal" data-bs-toggle="modal">
                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-database-add" viewBox="0 0 16 16">
                            <path d="M12.5 16a3.5 3.5 0 1 0 0-7 3.5 3.5 0 0 0 0 7Zm.5-5v1h1a.5.5 0 
                            0 1 0 1h-1v1a.5.5 0 0 1-1 0v-1h-1a.5.5 0 0 1 0-1h1v-1a.5.5 0 0 1 1 0Z"/>
                            <path d="M12.096 6.223A4.92 4.92 0 0 0 13 5.698V7c0 .289-.213.654-.753 
                            1.007a4.493 4.493 0 0 1 1.753.25V4c0-1.007-.875-1.755-1.904-2.223C11.022 
                            1.289 9.573 1 8 1s-3.022.289-4.096.777C2.875 2.245 2 2.993 2 4v9c0 1.007.875 
                            1.755 1.904 2.223C4.978 15.71 6.427 16 8 16c.536 0 1.058-.034 1.555-.097a4.525 
                            4.525 0 0 1-.813-.927C8.5 14.992 8.252 15 8 15c-1.464 0-2.766-.27-3.682-.687C3.356 
                            13.875 3 13.373 3 13v-1.302c.271.202.58.378.904.525C4.978 12.71 6.427 13 8 13h.027a4.552 
                            4.552 0 0 1 0-1H8c-1.464 0-2.766-.27-3.682-.687C3.356 10.875 3 10.373 3 10V8.698c.271.202.58.378.904.525C4.978 
                            9.71 6.427 10 8 10c.262 0 .52-.008.774-.024a4.525 4.525 0 0 1 1.102-1.132C9.298 8.944 8.666 9 8 9c-1.464 
                            0-2.766-.27-3.682-.687C3.356 7.875 3 7.373 3 7V5.698c.271.202.58.378.904.525C4.978 6.711 6.427 7 8 
                            7s3.022-.289 4.096-.777ZM3 4c0-.374.356-.875 1.318-1.313C5.234 2.271 6.536 2 8 2s2.766.27 
                            3.682.687C12.644 3.125 13 3.627 13 4c0 .374-.356.875-1.318 1.313C10.766 5.729 9.464 6 8 6s-2.766-.27-3.682-.687C3.356 
                            4.875 3 4.373 3 4Z"/>
                        </svg>
                    </button>
                </div>
                <thead>
                    <tr class="table-dark">
                        <th scope="col">Name</th>
                        <th scope="col">Description</th>
                        <th scope="col">Date</th>
                        <th scope="col" class="text-center">Edit</th>
                        <th scope="col" class="text-center">Delete</th>
                    </tr>
                </thead>
                <tbody id="partner-table-body">
                    <!-- Partner Data -->
                </tbody>
            </table>
            <nav aria-label="Page navigation">
                <ul class="pagination justify-content-center" id="pagination">
                    <!-- Pagination -->
                </ul>
            </nav>
        </div>

        <!-- Modal for Add/Edit Partners -->
        <div class="modal fade" id="partnerModal" tabindex="-1" aria-labelledby="partnerModalLabel" aria-hidden="true">
            <div class="modal-dialog modal-xl">
                <div class="modal-content">
                    <div class="modal-header" id="partnerModalHeader">
                        <!-- Insert -->
                    </div>
                    <div class="modal-body" id="insertInModalBody">
                        <!-- Insert -->
                    </div>
                    <div class="modal-footer" id="insertInModalFooter">
                        <!-- Insert -->
                    </div>
                </div>
            </div>
        </div>

        <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

        <script>
            let currentPage = 1;
            let searchTerm = '';
            const itemsPerPage = 10;

            $(document).ready(function () {
                function fetchPartnerData() {
                    const urlParams = new URLSearchParams(window.location.search);

                    currentPage = parseInt(urlParams.get('page')) || currentPage;
                    searchTerm = urlParams.get('search') || searchTerm;
                    $('#searchInput').val(searchTerm);

                    $.get(`<%= BACKEND_URL %>/api/partner/all?search=${searchTerm}&page=${currentPage}&itemsPerPage=${itemsPerPage}`, function (data) {
                        const partners = data.partners.partners;
                        const totalPages = data.totalPages;
                        const totalItems = data.partners.totalCount;

                        const tableBody = document.getElementById('partner-table-body');
                        tableBody.innerHTML = '';

                        partners.forEach(function (partner) {
                            const createdAt = new Date(partner.createdAt);
                            const year = createdAt.getFullYear();
                            const month = (createdAt.getMonth() + 1).toString().padStart(2, '0');
                            const day = createdAt.getDate().toString().padStart(2, '0');
                            const formattedDate = `${year}-${month}-${day}`;
                            let fileName = null;

                            if (partner.iconPath !== null) {
                                fileName = partner.iconPath.split('\\').pop();
                            }

                            const row = document.createElement('tr');
                            row.innerHTML = `
                                <td>${partner.name}</td>
                                <td class="text-truncate"  style="max-width: 150px;">${partner.description}</td>
                                <td>${formattedDate}</td>
                                <td class="text-center">
                                    <button class="btn btn-outline-success edit-button"
                                        data-id="${partner.id}"
                                        data-name="${partner.name}"
                                        data-description="${partner.description}"

                                        data-bs-toggle="modal"
                                        data-bs-target="#partnerModal"
                                        data-icon-url="/files/icons/${fileName}">
                                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" 
                                        class="bi bi-pencil-square" viewBox="0 0 16 16">
                                            <path d="M15.502 1.94a.5.5 0 0 1 0 .706L14.459 3.69l-2-2L13.502.646a.5.5 0 0 1 .707 0l1.293 1.293zm-1.75 
                                            2.456-2-2L4.939 9.21a.5.5 0 0 0-.121.196l-.805 2.414a.25.25 0 0 0 .316.316l2.414-.805a.5.5 0 0 
                                            0 .196-.12l6.813-6.814z"/>
                                            <path fill-rule="evenodd" d="M1 13.5A1.5 1.5 0 0 0 2.5 15h11a1.5 1.5 
                                            0 0 0 1.5-1.5v-6a.5.5 0 0 0-1 0v6a.5.5 0 0 1-.5.5h-11a.5.5 0 0 1-.5-.5v-11a.5.5 0 0 
                                            1 .5-.5H9a.5.5 0 0 0 0-1H2.5A1.5 1.5 0 0 0 1 2.5v11z"/>
                                        </svg>
                                    </button>
                                </td>
                                <td class="text-center">
                                    <button class="btn btn-outline-danger delete-button" 
                                        type="button" 
                                        data-id="${partner.id}">
                                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-trash3" viewBox="0 0 16 16">
                                            <path d="M6.5 1h3a.5.5 0 0 1 .5.5v1H6v-1a.5.5 0 0 1 .5-.5ZM11 2.5v-1A1.5 1.5 0 0 0 9.5 0h-3A1.5 1.5 
                                            0 0 0 5 1.5v1H2.506a.58.58 0 0 0-.01 0H1.5a.5.5 0 0 0 0 1h.538l.853 10.66A2 2 0 0 0 4.885 16h6.23a2 
                                            2 0 0 0 1.994-1.84l.853-10.66h.538a.5.5 0 0 0 0-1h-.995a.59.59 0 0 0-.01 0H11Zm1.958 1-.846 10.58a1 
                                            1 0 0 1-.997.92h-6.23a1 1 0 0 1-.997-.92L3.042 3.5h9.916Zm-7.487 1a.5.5 0 0 1 .528.47l.5 8.5a.5.5 0 
                                            0 1-.998.06L5 5.03a.5.5 0 0 1 .47-.53Zm5.058 0a.5.5 0 0 1 .47.53l-.5 8.5a.5.5 0 1 1-.998-.06l.5-8.5a.5.5 
                                            0 0 1 .528-.47ZM8 4.5a.5.5 0 0 1 .5.5v8.5a.5.5 0 0 1-1 0V5a.5.5 0 0 1 .5-.5Z"/>
                                        </svg>
                                    </button>
                                </td>
                            `;
                            tableBody.appendChild(row);
                        });

                        updatePagination(totalPages);
                    });
                }

                function updateURL() {
                    const urlParams = new URLSearchParams(window.location.search);
                    urlParams.set('page', currentPage);
                    urlParams.set('search', searchTerm);
                    history.pushState({}, '', `${window.location.pathname}?${urlParams}`);
                }

                $('#partner-table-body').on('click', '.edit-button', function () {
                    let iconUrl = $(this).data('icon-url');
                    check = iconUrl.split('/').pop();
                    
                    if (check === "null"){
                        iconUrl = "<%= BACKEND_URL %>/assets/upload.svg"
                    }
                    let partnerId = $(this).data('id');
                    let partnerName = $(this).data('name');
                    let partnerDescription = $(this).data('description');

                    const insertInModalHeader = document.getElementById('partnerModalHeader');
                    insertInModalHeader.innerHTML = `
                        <h3>Edit Partner</h3>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                    `;

                    const insertInModalBody = document.getElementById('insertInModalBody');
                    insertInModalBody.innerHTML = `
                        <div class="container">
                            <div class="row">
                                <div class="col d-flex flex-column">
                                    <label for="floatingTextarea">Name</label>
                                    <div class="input-group my-1">
                                        <span class="input-group-text">
                                            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" 
                                            class="bi bi-person-workspace" viewBox="0 0 16 16">
                                                <path d="M4 16s-1 0-1-1 1-4 5-4 5 3 5 4-1 1-1 1H4Zm4-5.95a2.5 2.5 0 1 0 0-5 2.5 2.5 0 0 0 0 5Z"/>
                                                <path d="M2 1a2 2 0 0 0-2 2v9.5A1.5 1.5 0 0 0 1.5 14h.653a5.373 5.373 0 0 1 1.066-2H1V3a1 1 
                                                0 0 1 1-1h12a1 1 0 0 1 1 1v9h-2.219c.554.654.89 1.373 1.066 2h.653a1.5 1.5 0 0 0 1.5-1.5V3a2 
                                                2 0 0 0-2-2H2Z"/>
                                            </svg>
                                        </span>
                                        <input id="inputAddPartner" type="text" class="form-control" placeholder="Partner" 
                                        aria-label="Partner" aria-describedby="input-group-left">
                                    </div>
                                    <label for="floatingTextarea">Description</label>
                                    <textarea id="inputAddDescription" class="form-control h-100" placeholder="Write a description"></textarea>
                                </div>
                                <div class="col-4 d-flex flex-column mt-1">
                                    <label for="floatingTextarea">Icon</label>
                                    <div class="card h-100">
                                        <div class="card-body d-flex flex-column justify-content-center align-items-center">
                                            <img id="selectedImage" src="${iconUrl}" class="card-img-top my-1" width="200" height="200">
                                            <label for="iconFile" class="btn btn-outline-dark">
                                                Upload an Icon
                                            </label>
                                            <input class="form-control" type="file" id="iconFile">                                            
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    `;

                    const insertInModalFooter = document.getElementById('insertInModalFooter');
                    insertInModalFooter.innerHTML = '';
                    insertInModalFooter.innerHTML = `   
                        <button type="submit" class="btn btn-success partner-edit-button">
                            Confirm
                        </button>
                        <button class="btn btn-outline-danger" data-bs-dismiss="modal">
                            Cancel
                        </button>
                    `;

                    const iconFile = document.getElementById("iconFile");
                    const selectedImage = document.getElementById("selectedImage");

                    iconFile.addEventListener("change", function () {
                        const file = iconFile.files[0];

                        if (file) {
                            if (file.type.startsWith("image/")) {
                                const reader = new FileReader();
                                reader.onload = function (e) {
                                    selectedImage.src = e.target.result;
                                };

                                reader.readAsDataURL(file);
                            } else {
                                alert("Please select an image file.");
                                iconFile.value = "";
                            }
                        }
                    });

                    $('.partner-edit-button').data('partner-id', partnerId);
                    $('#inputAddPartner').val(partnerName);
                    $('#inputAddDescription').val(partnerDescription);

                });

                $('#insertInModalFooter').on('click', '.partner-edit-button', function () {
                    const partnerId = $('.partner-edit-button').data('partner-id');
                    const name = $('#inputAddPartner').val();
                    const description = $('#inputAddDescription').val();

                    const fileInput = document.getElementById('iconFile');
                    const file = fileInput.files[0];

                    const formData = new FormData();
                    formData.append('name', name);
                    formData.append('description', description);
                    formData.append('fileInput', file);

                    const formDataJSON = {};
                    formData.forEach((value, key) => {
                    formDataJSON[key] = value;
                    });

                    $.ajax({
                        url: `<%= BACKEND_URL %>/api/partner/update/${partnerId}`,
                        type: 'PUT',
                        data: formData,
                        processData: false, 
                        contentType: false, 
                        success: function (response) {
                            console.log('Partner updated successfully:', response);
                            $('#partnerModal').modal('hide');
                            fetchPartnerData();
                        },
                        error: function (error) {
                            console.error('Error updating partner:', error);
                        }
                    });
                });

                $('#partnerCaption').on('click', '.add-button', function () {
                    const insertInModalHeader = document.getElementById('partnerModalHeader');
                    insertInModalHeader.innerHTML = `
                        <h3>Add Partner</h3>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                    `;

                    const insertInModalBody = document.getElementById('insertInModalBody');
                    insertInModalBody.innerHTML = `
                        <div class="container">
                            <div class="row">
                                <div class="col d-flex flex-column">
                                    <label for="floatingTextarea">Name</label>
                                    <div class="input-group my-1">
                                        <span class="input-group-text">
                                            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-person-workspace" viewBox="0 0 16 16">
                                                <path d="M4 16s-1 0-1-1 1-4 5-4 5 3 5 4-1 1-1 1H4Zm4-5.95a2.5 2.5 0 1 0 0-5 2.5 2.5 0 0 0 0 5Z"/>
                                                <path d="M2 1a2 2 0 0 0-2 2v9.5A1.5 1.5 0 0 0 1.5 14h.653a5.373 5.373 0 0 1 1.066-2H1V3a1 1 
                                                0 0 1 1-1h12a1 1 0 0 1 1 1v9h-2.219c.554.654.89 1.373 1.066 2h.653a1.5 1.5 0 0 0 1.5-1.5V3a2 
                                                2 0 0 0-2-2H2Z"/>
                                            </svg>
                                        </span>
                                        <input id="inputAddPartner" type="text" class="form-control" placeholder="Write a name" aria-label="Partner" aria-describedby="input-group-left">
                                    </div>
                                    <label for="floatingTextarea">Description</label>
                                    <textarea id="inputAddDescription" class="form-control h-100" placeholder="Write a description" style="height: 200px"></textarea>
                                </div>
                                <div class="col-4 d-flex flex-column mt-1">
                                    <label for="floatingTextarea">Icon</label>
                                    <div class="card h-100">
                                        <div class="card-body d-flex flex-column justify-content-center align-items-center">
                                            <img id="selectedImage" src="<%= BACKEND_URL %>/assets/upload.svg" class="card-img-top my-1" width="200" height="200">
                                            <label for="iconFile" class="btn btn-outline-dark">
                                                Upload an Icon
                                            </label>
                                            <input class="form-control" type="file" id="iconFile">                                            
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    `;

                    const insertInModalFooter = document.getElementById('insertInModalFooter');
                    insertInModalFooter.innerHTML = '';
                    insertInModalFooter.innerHTML = `
                        <button type="submit" class="btn btn-success partner-add-button">
                            Confirm
                        </button>
                        <button class="btn btn-outline-danger" data-bs-dismiss="modal">
                            Cancel
                        </button>
                    `;

                    const iconFile = document.getElementById("iconFile");
                    const selectedImage = document.getElementById("selectedImage");

                    iconFile.addEventListener("change", function () {
                        const file = iconFile.files[0];

                        if (file) {
                            if (file.type.startsWith("image/")) {
                                const reader = new FileReader();
                                reader.onload = function (e) {
                                    selectedImage.src = e.target.result;
                                };
                                reader.readAsDataURL(file);
                            } else {
                                alert("Please select an image file.");
                                iconFile.value = "";
                            }
                        }
                    });
                });

                $('#insertInModalFooter').on('click', '.partner-add-button', function () {
                    const name = $('#inputAddPartner').val();
                    const description = $('#inputAddDescription').val();
      
                    const fileInput = document.getElementById('iconFile');
                    const file = fileInput.files[0];

                    const formData = new FormData();
                    formData.append('name', name);
                    formData.append('description', description);
                    formData.append('fileInput', file);

                    $.ajax({
                        url: '<%= BACKEND_URL %>/api/partner/create',
                        type: 'POST',
                        data: formData,
                        processData: false, 
                        contentType: false, 
                        success: function (response) {
                            console.log('Partner added successfully:', response);
                            $('#partnerModal').modal('hide');
                            fetchPartnerData();
                        },
                        error: function (error) {
                            console.error('Error adding partner:', error);
                        }
                    });
                });

                $('#searchForm').submit(function (e) {
                    e.preventDefault();
                    searchTerm = $('#searchInput').val();
                    currentPage = 1;
                    updateURL();
                    fetchPartnerData();
                });

                $('#resetSearchButton').click(function () {
                    $('#searchInput').val('');
                    searchTerm = $('#searchInput').val();
                    currentPage = 1;
                    updateURL();
                    fetchPartnerData();
                });

                function changePage(page) {
                    currentPage = page;
                    const urlParams = new URLSearchParams(window.location.search);
                    urlParams.set('page', page);
                    const newURL = `${window.location.pathname}?${urlParams}`;
                    window.history.pushState({ path: newURL }, '', newURL);
                    fetchPartnerData(page, searchTerm);
                }

                function updatePagination(totalPages) {
                    const pagination = document.getElementById('pagination');
                    pagination.innerHTML = '';

                    const previousItem = document.createElement('li');
                    previousItem.className = `page-item ${currentPage === 1 ? 'disabled' : ''}`;
                    const previousLink = document.createElement('a');
                    previousLink.className = 'page-link';
                    previousLink.href = 'javascript:void(0);';
                    previousLink.textContent = '«';
                    previousLink.onclick = () => changePage(currentPage - 1);
                    previousItem.appendChild(previousLink);
                    pagination.appendChild(previousItem);

                    for (let i = 1; i <= totalPages; i++) {
                        const pageItem = document.createElement('li');
                        pageItem.className = `page-item ${i === currentPage ? 'active' : ''}`;
                        const pageLink = document.createElement('a');
                        pageLink.className = 'page-link';
                        pageLink.href = 'javascript:void(0);';
                        pageLink.textContent = i;
                        pageLink.onclick = () => changePage(i);
                        pageItem.appendChild(pageLink);
                        pagination.appendChild(pageItem);
                    }

                    const nextItem = document.createElement('li');
                    nextItem.className = `page-item ${currentPage === totalPages ? 'disabled' : ''}`;
                    const nextLink = document.createElement('a');
                    nextLink.className = 'page-link';
                    nextLink.href = 'javascript:void(0);';
                    nextLink.textContent = '»';
                    nextLink.onclick = () => changePage(currentPage + 1);
                    nextItem.appendChild(nextLink);
                    pagination.appendChild(nextItem);
                }

                $('#partner-table-body').on('click', '.delete-button', function () {
                    const partnerId = $(this).data('id');
                    const rowCount = $('#partner-table-body tr').length;

                    $.ajax({
                        url: `<%= BACKEND_URL %>/api/partner/delete/${partnerId}`,
                        type: 'DELETE',
                        success: function () {
                            fetchPartnerData();

                            if ($('#partner-table-body tr').length === 0) {
                                updateURL();
                                fetchPartnerData();
                            }
                            if (rowCount === 1 && currentPage!==1) {
                                changePage(currentPage - 1);
                            }
                        },
                        error: function (error) {
                            console.error('Error deleting partner:', error);
                        },
                    });
                });

                fetchPartnerData();
            });

            document.getElementById('logout-button').addEventListener('click', function() {
                $.ajax({
                url: `<%= BACKEND_URL %>/api/logout/`,
                type: 'GET',
                success: function (response) {
                    console.log('Logged out successfully.');
                    window.location.href = '/login';
                },
                error: function (xhr, textStatus, error) {
                    console.error('Error logging out:', error);
                }
                });
            });
            
        </script>

    </body>
</html>

<style>
    .page-item.active .page-link {
        background-color: #212529;
        color: white;
    }

    .page-link {
        background-color: #E9ECEF;
        color: #212529;
    }

    .page-link:hover {
        background-color: #212529;
        color: white;
    }

  

    #iconFile {
        display: none;
    }

</style>